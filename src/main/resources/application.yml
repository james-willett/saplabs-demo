spring:
  security:
    user:
      name: user
      password: pass

server:
  port: 8080 # TODO - change back to 443 and uncomment the below ssl
#  ssl:
#    key-store: keystore.p12
#    key-store-password: Turkish@59
#    keyStoreType: PKCS12
#    keyAlias: tomcat